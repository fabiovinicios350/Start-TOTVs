Function Main()

    //Variaveis
    LOCAL nBase := 2.00
    LOCAL nAltura := 10.00
    LOCAL nResultado := (nBase*nAltura)/2

    //Apresentando na tela os resultados
    QOUT("Base: " + str(nBase))
    QOUT("Altura: " + str(nAltura))
    QOUT("resultado: " + str(nResultado))

Return nil
